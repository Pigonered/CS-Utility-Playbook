{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main and screenshot windows",
  "windows": ["main", "screenshot-overlay"],
  "permissions": ["core:default", "dialog:allow-open", "dialog:allow-save"]
}
